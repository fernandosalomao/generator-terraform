# The providers.tf file is used in Terraform to define the provider(s) used by your Terraform configuration. Providers are plugins that Terraform uses to interact with various infrastructure platforms and services, such as AWS, Azure, GCP, and many others.

provider "random" {
  # Configuration options
}